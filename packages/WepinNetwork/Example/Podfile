use_frameworks!

platform :ios, '10.0'

target 'WepinNetwork_Example' do
  pod 'WepinNetwork', :path => '../'
  pod 'WepinCommon', :path => '../../WepinCommon'

  target 'WepinNetwork_Tests' do
    inherit! :search_paths

    
  end
end
